package evm

import "testing"

func TestMessageProcessing(t *testing.T) {
	for _, tt := range []struct {
		name string
	}{
		{
			name: "bla",
		},
	} {
		t.Run(tt.name, func(t *testing.T) {
			// c := Compass{}
		})
	}
}
